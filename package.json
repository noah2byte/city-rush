{
  "name": "city-rush",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "assets": "node scripts/build-assets.mjs",
    "predev": "node scripts/build-assets.mjs --if-missing",
    "dev": "vite",
    "prebuild": "node scripts/build-assets.mjs --if-missing",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "@gltf-transform/cli": "^4.1.0"
  }
}